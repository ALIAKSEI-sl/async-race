import { ControllingDirective } from './controlling.directive';

describe('ControllingDirective', () => {
	it('should create an instance', () => {
		const directive = new ControllingDirective();
		expect(directive).toBeTruthy();
	});
});
